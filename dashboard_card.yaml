# Dashboard card - paste as Manual card in your dashboard
# This card combines input, model selection, and response into a single unified UI.
# The frontend resource is automatically registered by the integration.
type: custom:llm-card
response_entity: sensor.llm_ha_model_response
